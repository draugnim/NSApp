android {
            defaultConfig {
                applicationId = "org.nativescript.rupagis"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }